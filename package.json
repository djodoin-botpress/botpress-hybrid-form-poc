{
  "name": "botpress-hybrid-form-poc",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "PoC: hybrid contact form (classic form + AI-driven follow-ups) on the Botpress Chat API",
  "engines": {
    "node": ">=20.12"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js"
  },
  "dependencies": {
    "@botpress/chat": "^1.1.0",
    "express": "^4.21.2"
  }
}
